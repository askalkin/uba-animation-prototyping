# Red magnifier

**Group:** Search  
**Slug:** `search-red-magnifier-loader`  
**Status:** Ready + image  
**Target format:** Lottie + image

## Playback contract

loop, `loop`, cycle 4600ms

## Required images

Ship these alongside the animation file at @2x/@3x:

- `shared/assets/search-magnifier-red.webp`

## Notes

Single 4.6s loop. An earlier 4.4s flag belonged to .search-spinner-3-scene, which is not part of this animation.

## Web reference

`reference/preview.html` — open in a browser to see the exact source motion.
