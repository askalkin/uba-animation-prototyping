# Orbit magnifier

**Group:** Search  
**Slug:** `search-red-orbit-magnifier`  
**Status:** Ready + image  
**Target format:** Lottie + image

## Playback contract

loop, `loop`, cycle 9200ms

## Required images

Ship these alongside the animation file at @2x/@3x:

- `shared/assets/search-magnifier-red.webp`

## Notes

Ring periods harmonised to 4.6 / 6.9 / 9.2s (ratio 1 : 1.5 : 2). The scene now repeats every 27.6s, down from ~30 minutes. A 2.3 / 4.6 / 9.2s set would bring that to 9.2s if a shorter export is needed.

## Loop periods in the web source

4.6s, 6.9s, 9.2s — these run independently and do not realign quickly.

## Web reference

`reference/preview.html` — open in a browser to see the exact source motion.
